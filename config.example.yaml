http:
  # Use a non-sensitive environment endpoint (replace locally)
  base_url: https://api.example.com
  # Keep empty in source control; inject via local config/CI secrets
  api_key:
  device_meta_data: generic-device
  user_phone_number:

swagger:
  url: https://api.example.com
  modules:
    - path: shopping_order
      url: shoppingorder

    - path: user_manager
      url: usermanager

    - path: domestic_flight_aggregator
      url: domestic-flight-aggregator


databases:
  - name: mysql
    host:
    port:
    username:
    password:
    database:

modules:
  domestic:
    passenger_id: 00000000-0000-0000-0000-000000000000
    mock_airline: "SAMPLE_AIRLINE"
    discount_code:
    flightTotalPrice: 0